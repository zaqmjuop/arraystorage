{
  "author": "zaqmjuop",
  "name": "arraystorage",
  "version": "1.0.1",
  "description": "a simple helper for use indexeddb easily just like Array",
  "repository": {
    "type": "git",
    "url": "https://github.com/zaqmjuop/arraystorage.git"
  },
  "engines": {
    "node": "*"
  },
  "directories": {},
  "main": "docs/arraystorage.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html -p 3000",
    "buildjs": "parcel build index.js -d ./docs && parcel build arraystorage.js -d ./docs",
    "update": "npm run buildjs && npm publish"
  },
  "keywords": [],
  "license": "ISC",
  "presets": [
    "env"
  ],
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^13.0.0",
    "eslint-plugin-import": "^2.13.0"
  }
}
